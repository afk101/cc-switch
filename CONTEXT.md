# CC Switch

CC Switch 管理多个 AI 客户端配置及其本地路由生命周期。本词汇表统一项目中涉及 Profile 路由的领域语言。

## Language

**Profile 路由关闭**：
用户显式结束指定 Profile 路由接管状态的生命周期动作。关闭立即停止接收新请求，不等待也不主动取消已经进入路由的请求。
_Avoid_: 优雅关闭、排空关闭

**在途路由请求**：
Profile 路由关闭前已经进入转发过程的请求。它可以在路由关闭后自行结束，但关闭流程不等待其结果。
_Avoid_: 待排空请求
